# Distributed system with go

## Install rabbitmq
`brew update`
`brew install rabbitmq`

1. to start the server: `rabbitmq-server`
2. to check status: `rabbitmqctl status`
3. to stop server: `rabbitmqctl stop`